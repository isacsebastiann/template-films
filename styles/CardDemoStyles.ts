// CardDemoStyles.ts
import { styleSheet } from 'tamagui';

// CardDemoStyles.ts
export const styles = {
    xStack: {
      $sm: { flexDirection: 'column' },
      paddingHorizontal: '$10',
      alignItems: 'center',
    },
    demoCard: {
      backgroundColor: '#570838',
      marginTop: 25,
      width: 340,
      height: 150,
      borderRadius: 20,
    },
    cardHeader: {
      color: '#fff', // Considera usar variables de tema aquí.
    },
    // Agrega más estilos según sea necesario.
  };
  
